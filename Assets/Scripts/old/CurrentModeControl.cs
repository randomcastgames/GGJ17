using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class CurrentModeControl : TranslatableObject {
/*
    void OnEnable()
    {
        if (GameManager.instance.currentGameMode == GameMode.RIGHT_HANDED)
            term = "RightHanded";
        else
            term = "LeftHanded";

        base.Start();
    }

    public void ChangeGameMode()
    {
        if (GameManager.instance.currentGameMode == GameMode.RIGHT_HANDED)
        {
            term = "LeftHanded";
            GameManager.instance.currentGameMode = GameMode.LEFT_HANDED;
        }
        else
        {
            term = "RightHanded";
            GameManager.instance.currentGameMode = GameMode.RIGHT_HANDED;
        }

        ApplyTerm();

        PlayerPrefs.SetInt("GameMode", (int)GameManager.instance.currentGameMode);
    }
    */
}
